<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/C14-Tech-Blog/assets/css/jass.css">
  <link rel="stylesheet" href="/C14-Tech-Blog/assets/css/style.css">
</head>

<body>
  <div class="flex-column">
    <header class="display-flex justify-space-between align-center">
      <h1>
        Tech Blog
      </h1>
      <nav>
        <a href="/">Home</a> |
        <a href="/dashboard">Dashboard</a> |
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
    </header>

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/C14-Tech-Blog/assets/js/logout.js"></script>
  {{/if}}
</body>

</html>